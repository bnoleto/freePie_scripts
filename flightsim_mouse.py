if starting:
	system.setThreadTiming(TimingTypes.HighresSystemTimer)
	system.threadExecutionInterval = 5


if(mouse.wheelUp) :

	keyboard.setPressed(Key.F3)
	
if(mouse.wheelDown) :
	keyboard.setPressed(Key.F2)
